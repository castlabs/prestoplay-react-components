
import { Canvas, Meta, Primary, Controls, Story, ArgTypes } from '@storybook/blocks';
import * as VolumeBarStories from './VolumeBar.stories';

<Meta of={VolumeBarStories} name="Docs" />

<Canvas of={VolumeBarStories.Primary} />

# Volume Bar

A component to increase/decrease volume by dragging the slider.

### Props

<ArgTypes of={VolumeBarStories.Primary} />
