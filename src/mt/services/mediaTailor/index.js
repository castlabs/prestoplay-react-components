import { Session } from './session'
import { initialize } from './initialization'

/**
 * @fileoverview MediaTailor service.
 * @see {@link https://docs.aws.amazon.com/mediatailor/latest/ug/ad-reporting-client-side.html | API}
 */

export const Mt = {
  initialize,
  Session,
}
